{
  "version": 2,
  "builds": [
    { "src": "app.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/users/(.*)", "dest": "/app.js" },
    { "src": "/api/providers/(.*)", "dest": "/app.js" },
    { "src": "/api/report/(.*)", "dest": "/app.js" }
  ]
}
